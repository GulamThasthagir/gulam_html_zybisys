// 1.FIND SUM

  /**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(nums, target) {
        let out=[]
        for (let i=0;i<nums.length;i++){

        for (let j=i+1;j<nums.length;i++){
            let a=nums[i]
        let b=nums[j]
        let c=a+b
        if(c==target){
            return[i,j]
        }
        }
        }
    }

// 2. Find position

var searchRange = function(nums, target) {
    let out=[]
        for (let i=0;i<nums.length;i++){
        for(let j=i+1;j<nums.length;i++){
            let a=nums[i]
            let b=nums[j]
            let c=a+b
            if(c==target)
            console.log(c)
        }
        } 
//3.
var firstMissingPositive = function(nums) {
    for(let i=0;i<nums.length;i++)
    let find (-nums)[i];
    if(obj[find]=undifined)
    obj()
};
